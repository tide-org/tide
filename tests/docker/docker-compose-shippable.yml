version: "2"

services:

  test-python-tide:
    build:
      context: ..
      dockerfile: ./docker/Dockerfile
    working_dir: /work
    tmpfs: /tests
    environment:
      - PYTHONPATH=/work
    volumes:
      - temp-shippable-volume:/work

volumes:
  temp-shippable-volume:
    driver: local
