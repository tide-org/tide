version: "2"

services:

  test-python-tide:
    build:
      context: ..
      dockerfile: ./docker/Dockerfile
    working_dir: /work
    tmpfs: /tests
    volumes:
      - ../..:/work
      - ../../../tide-plugins/plugins:/work/plugins
