version: "2"

services:

  test-python-tide:
    build:
      context: ../docker
      dockerfile: Dockerfile-tests
    working_dir: /work/tide/tests/scripts
    environment:
      - PYTHONPATH=/work/tide
    volumes:
      - ../..:/work/tide
      - ../../plugins:/work/tide/plugins/plugins
  pylint:
    build:
      context: ../docker
      dockerfile: Dockerfile-pylint
    working_dir: /work/tide
    volumes:
      - ../..:/work
